# AURA Komponenten-Referenz

## VideoCard
- `<video-card>`  
- props: `data.src`  
- autoplay, muted, object-fit cover  

## VideoDeck
- `<video-deck>`  
- JSON items → Swipe-fähige Karten  
- GPU-first, zIndex stacking  

## VideoGrid
- `<video-grid>`  
- Scrollbares Grid  
- Klick → Overlay Fullscreen


---

💡 **So legst du es im Repo ab:**

1. `docs/` Ordner erstellen
2. Alle `.md` Dateien einfügen
3. Commit & pushen:

```bash
git add docs/
git commit -m "docs: offizielle AURA-Dokumentation (Human + AI)"
git push origin feature/aura-video-deck
```

---
